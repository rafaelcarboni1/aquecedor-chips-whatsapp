{
  "name": "mirage-whatsapp-orchestrator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"pnpm --filter dashboard dev\" \"pnpm --filter orchestrator dev\"",
    "build": "pnpm --filter dashboard build && pnpm --filter orchestrator build",
    "lint": "pnpm --filter '*' lint",
    "type-check": "pnpm --filter '*' type-check",
    "clean": "pnpm --filter '*' clean"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "concurrently": "^9.2.0",
    "eslint": "^9.25.0",
    "prettier": "^3.0.0",
    "typescript": "~5.8.3"
  }
}
